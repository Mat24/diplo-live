<!DOCTYPE html>
<html>
<head>
  <title>Gemsapp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <div class="pull-right">
    <nav>
      <ul class="nav navbar-default">
        <% if user_signed_in? %>
            <li>
              Logged in as <strong><%= current_user.email %></strong>.
            </li>
            <li>
              <%= link_to 'Editar mi cuenta', edit_user_registration_path, :class => 'navbar-link' %>
            </li>
            <li>
              <%= link_to "Cerrar sesion", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
            </li>
        <% else %>
            <li>
              <%= link_to "Crear cuenta", new_user_registration_path, :class => 'navbar-link'  %> |
            </li>
            <li>
              <%= link_to "Iniciar sesion", new_user_session_path, :class => 'navbar-link'  %>
            </li>

        <% end %>
      </ul>
     </nav>
  </div>
  <div class="container">
    <div class="jumbotron">
      <%= yield %>
    </div>
  </div>
</div>
</body>
</html>
